# AI Workflow Portfolio

Case studies in AI automation — n8n, Claude, and modern infrastructure.

## Structure

```
├── index.html                          # Landing page
├── case-studies/
│   └── contact-form-sorter/
│       ├── index.html                  # Interactive case study
│       └── workflow.json               # n8n workflow export
│   └── [next-project]/
│       ├── index.html
│       └── workflow.json
```

Each case study lives in its own folder under `case-studies/` with a consistent structure.
